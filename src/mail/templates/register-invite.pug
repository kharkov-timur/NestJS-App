doctype html
html
    head
        style.
            .confirmedWindowContainer {
                width: 100%;
                height: 100%;
                padding-bottom: 5%;
                background-color: whitesmoke;

            }

            .confirmedText {
                text-align: center;
                font-size: 20px;
                color: black;
                margin: auto;
                padding-top: 10%;
                justify-content: center;
            }

            .confirmedText a {
                color: black;
            }

            .confirmed {
                text-align: center;
                margin-top: 5%;
            }

            button {
                border: none;
                margin: auto;
                background-color: #26bd62;
                padding: 10px 30px;
                font-size: 18px;
                color: #ebf2f2;
                min-width: 125px;
                max-height: 107px;
                height: 48px;
                cursor: pointer;
            }

            a {
                font-size: 18px;
                color: white;
            }

            .header {
                height: 40px;
                width: 100%;
                background-color: #26bd62;
            }

    body
        div.confirmedWindowContainer
            div.header
            div.confirmedText Ви успішно пройшли реєстрацію в
                | !{' '}
                a(href=`${frontUrl}`) магазині Buy All
                p Для завершення реєстрації перейдіть за посиланням:
            div.confirmed
                a(href=`${url}`)
                    button Підтвердити email